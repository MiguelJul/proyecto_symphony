{% extends 'base.html.twig' %}
{% block title %}Editar equipo{% endblock %}
{% block stylesheets %}
{{ parent() }}
 <link href="{{ asset('Green/assets/css/estilsEquip.css') }}" rel="stylesheet">
{% endblock %}
{% block javascripts %}
{{ parent() }}
{% endblock %}
{% block header %}
{{ parent() }}
{%endblock%}
{% block body %}
 <div id="paragrafEquip">
{% if error %}
<div class="exception">{{error}}</div>
{% endif %}
    <h1>Editar membre</h1>
    {{ form_start(formulari) }}
    {{ form_widget(formulari) }}
    {{ form_end(formulari) }}
    </div>
     <div id="paragrafEquip"><img class="imatgeEquip" src="{{ asset('Green/assets/img/membres/'~imatge)}}"/></div>
    <div id="paragrafEquip"><a class="nav-link" href ="{{ path('inici') }}">Tornar a l'inici</a></div>
{%endblock%}

{% block footer %}
{{ parent() }}
{%endblock%}
